# Hardware Monitoring Requirements
# Optional dependencies for advanced CPU/GPU temperature monitoring
# Only install these if you want hardware monitoring features

# HTTP-based LibreHardwareMonitor access (cross-platform, no admin needed)
requests>=2.31.0

# NVIDIA GPU monitoring (optional, GPU-only, no admin needed)
nvidia-ml-py3>=7.352.0

# Note: Hardware monitoring features:
# - Require explicit user consent before enabling
# - GPU-only mode works without administrator privileges
# - CPU monitoring requires admin rights on Windows
# - All monitoring is local-only (no external data transmission)